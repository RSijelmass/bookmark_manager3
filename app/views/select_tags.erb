<% @links.each do |link| %>

    <% link.tags.each do |tag|
      if tag.name == @tag %>
        <li>
          Title: <%= link.title %>
          URL: <%= link.url %>
        </li>
      <% end %>
    <% end %>

<% end %>

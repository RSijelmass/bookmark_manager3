<% @links.each do |link| %>
  <li>
    <% link.tags.each do |tag|
      if tag.name == 'shopping' %>

        Title: <%= link.title %>
        URL: <%= link.url %>
      <% end %>
    <% end %>
  </li>
<% end %>
